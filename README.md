## Data Engineering Task 1



Takes invalid ruby style Json file and converts to Json format, then feeds the data inside to a database following which creates a summary table from that database.
